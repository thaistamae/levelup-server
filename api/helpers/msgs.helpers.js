const invalidPasswordRequirementsMsg =
  "Password is required and must have at least 8 characters, uppercase and lowercase letters, numbers and special characters.";

const invalidEmailMsg = "This email is not yet registered in our website";

const invalidPasswordOrEmailMsg = "Wrong password or email";

const invalidUserMsg = "User not found";

const invalidToken = "Incorrect or expired token!";

const emailAlreadyRegisteredMsg = "This email is already registered";

const successMsg = "Ok";

module.exports = {
  invalidPasswordRequirementsMsg,
  invalidEmailMsg,
  invalidPasswordOrEmailMsg,
  invalidUserMsg,
  invalidToken,
  emailAlreadyRegisteredMsg,
  successMsg,
};
